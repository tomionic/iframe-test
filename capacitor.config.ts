import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capture-app-iframe-case',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
